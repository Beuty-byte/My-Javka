package answer;

public class ThirdAnswer {


    public static double thirdTask(int x, int y)
    {
        return (Math.sin(x) + Math.cos(y)) / (Math.cos(x) - Math.sin(y)) * ((Math.sin(x)/Math.cos(y)));


        //tg = sin/cos xy
    }
}
